---
title: 记录一些比较难的算法题
date: 2025-08-13 19:50:20
categories:
 - 面试
tags:
 - 面试
 - 算法面试
---

**207 课程表**

```python
class Solution:
    def canFinish(self, numCourses, prerequisites):
        # 1. 构建邻接表 & 入度表
        adjacency = defaultdict(list)
        in_degree = [0] * numCourses
       
        for dest, src in prerequisites:
            adjacency[src].append(dest)
            in_degree[dest] += 1
       
        # 2. 初始化队列（入度为0的课程）
        queue = deque()
        learned_count = 0
        for i in range(numCourses):
            if in_degree[i] == 0:
                queue.append(i)
               
         # 3. Kahn 拓扑排序
        while queue:
            course = queue.popleft()
            learned_count += 1
           
            for next_course in adjacency[course]:
                in_degree[next_course] -= 1
                if in_degree[next_course] == 0:
                    queue.append(next_course)
                   
        # 4. 如果学完的课程数等于总课程数 → 没有环
        return learned_count == numCourses
```

**208. 实现 Trie (前缀树)**

```python
class Trie:
    def __init__(self):
        self.children = [None] * 26
        self.isEnd = False
    
    def searchPrefix(self, prefix: str) -> "Trie":
        current = self
        for ch in prefix:
            index = ord(ch) - ord("a")
            if not current.children[index]:
                return None
            current = current.children[index]
        return current

    def insert(self, word: str) -> None:
        current = self  # 当前位置 根节点
        for char in word:
            index = ord(char) - ord("a")
            if not current.children[index]:
                current.children[index] = Trie()
            current = current.children[index]  # 移动到下一个位置
        current.isEnd = True

    def search(self, word: str) -> bool:
        node = self.searchPrefix(word)
        return node is not None and node.isEnd

    def startsWith(self, prefix: str) -> bool:
        return self.searchPrefix(prefix) is not None
```
